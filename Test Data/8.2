8.2 push and pop
8.2 pop - to remove item<br>push - to insert an element<br>
8.2 Pop and push
8.2 pop and push
8.2 push - adds an item to the top of the stack<br>pop - removes the top item from the stack
8.2 push and pop.<br>Push adds a given node to the top of the stack leaving previous nodes below. Pop removes and returns the current top node of the stack
