6.3 It dereferences a pointer, meaning it returns the value stored in the memory address a pointer refers to.
6.3 It is unary operator that returns the value of the variable located at the address
6.3 a reference to a = variable
6.3 a synonym for the object to which its pointer operand points
6.3 it returns the actual data at the address pointed to
6.3 will return zero or many characters placed before the operator 
