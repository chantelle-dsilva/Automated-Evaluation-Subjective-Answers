8.3 Allocate an array of some size, bottom stack element stored at element 0
8.3 you store the stack in the array but you have to keep in mind the first element
8.3 You keep track of the last item entered into the array and do not<br> allow the user access to the other parts of the array.
8.3 using arrays, you are pushing one int at a time into the stack until you get to the end of your array
8.3 by having a max number of elements, allocating an array with that number of elements, storing the elements starting at 0, and keeping track of how many elements are currently in the list
8.3 Stack usually holds a bunch of items with the same type, therefore u can use an array, to hold the content of the stack and an integer top that holds the index of the element at the top of the stack. u Choose an array size. Then the sequence of operations used. u have to decide what data types are needed for this stack data structure. 
